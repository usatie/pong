version: "3.9"
services:
  web:
    build: web
    ports:
      - "${NGINX_PORT}:80"
    depends_on:
      - backend
      - frontend
  frontend:
    build: frontend
    command: npm run dev
    environment:
      PORT: ${FRONTEND_PORT}
      API_URL: http://backend:${BACKEND_PORT}/api
      NEXT_PUBLIC_API_URL: http://${HOSTNAME}:${NGINX_PORT}/api
    depends_on:
      backend:
        condition: service_started
      db:
        condition: service_healthy
  backend:
    build: backend
    command: sh -c "yarn prisma migrate deploy && yarn start:dev"
    environment:
      PORT: ${BACKEND_PORT}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    depends_on:
      db:
        condition: service_healthy
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${POSTGRES_USER}"]
      interval: 1s
      timeout: 5s
      retries: 30
