services:
  web:
    volumes:
      - ./web/html:/var/www/playground
      - ./web/nginx.conf:/etc/nginx/nginx.conf
  backend:
    command: yarn start:dev
    volumes:
      - ./backend:/app
  frontend:
    command: npm run dev
    volumes:
      - ./frontend:/app
